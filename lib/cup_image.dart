import 'package:flutter/material.dart';

class CupImage extends StatelessWidget {
  const CupImage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(
      Icons.local_drink,
      color: Colors.black,
      size: 30.0,
    );
  }
}